将 Arduino 编译输出的 Serial_CPU_Freqs.ino.bin（或重命名为与本仓库 version.json 中 url 一致）放到本目录，
提交并推送到 GitHub 后，jsDelivr 即可通过 version.json 中的 cdn.jsdelivr.net/gh/... URL 分发固件。
勿仅上传到 GitHub Release 而不提交到仓库——否则设备无法用 jsDelivr 拉取。
